# Runtime

This directory is reserved for the runtime subsystem. The initial working implementation lives in the language-neutral Python package under `sugi/` while the repository keeps top-level subsystem boundaries explicit for future adapters and generated bindings.
