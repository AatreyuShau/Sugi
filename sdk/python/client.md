# SUGI SDK client

Thin protocol wrapper only. SDKs send commands such as `query`, `set_property`, `set_variable`, `dispatch_event`, `play_animation`, `subscribe`, and `unsubscribe`; they do not duplicate runtime logic.
